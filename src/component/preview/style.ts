import styled from 'styled-components';

export const PreviewBox = styled.div`
  position: relative;

  .btn-group {
    > button {
      margin-right: 10px;
    }
  }

  .preview-img {
    margin-top: 20px;
    width: 100%;
  }
`;
